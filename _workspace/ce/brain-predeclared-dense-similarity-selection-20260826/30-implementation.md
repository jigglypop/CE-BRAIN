# Implementation

Status: COMPLETE

`predeclared_dense_similarity_selection.py` adds immutable candidate/menu/result
records, exact invertibility and scalar normalization, canonical hashing, unchanged
menu reconstruction, dense-only development scoring, unique winner/runner and
strict advantage gates, one selected heldout call, and separate honesty flags.

It composes the unchanged dense residual verifier and cannot bypass any transformed
uncertainty, condition-number, node, or chord gate.
