# Stage 2 successor negative controls

Status: PREREGISTERED / OUTCOME_BLIND

- Trialwise L2 normalization removes positive whole-waveform amplitude scale.
- The nonnegative global-gain residual checks whether a mean amplitude-only map
  is sufficient at the registered resolution.
- Currents are never pooled or directly contrasted across mice.
- The same common 21 series columns and time grid are used in all six
  animal/state cells.
- Development trials may expose apparatus faults only; they may not alter the
  endpoint, seeds, thresholds or decision.
- Recovery is compared with the same animal's awake block. A result is not
  described as causal recovery because temporal order remains confounded.
- A mouse-label swap and a within-state trial shuffle are implementation
  mutation controls; either changing a frozen decision receipt stops the run.

