# Stage 2 successor gates

Status: PREREGISTERED / OUTCOME_BLIND

1. Both published DANDI identities, paths, byte sizes and SHA-256 values match.
2. The registered EEG shapes, conversion, timestamp profile, common 21 series
   columns, trial fields and even/odd counts reproduce the schema receipts.
3. Preprocessing returns exactly 249 time samples by 21 channels for every
   eligible trial and every waveform has a positive finite norm.
4. Tests cover channel intersection, timing, artifact substitution, gain fit,
   distance ratio, permutation determinism, bootstrap determinism and all
   decision branches.
5. Contract, source lock, hypotheses, models, metrics, gates, negative controls,
   code, tests and schema receipts are hashed before any confirmation EEG value
   is opened.
6. One-shot execution refuses overwrite and a separate raw-data recomputation
   must reproduce the canonical result exactly.

Any failure is an apparatus stop and cannot be converted into scientific
support or tension.

