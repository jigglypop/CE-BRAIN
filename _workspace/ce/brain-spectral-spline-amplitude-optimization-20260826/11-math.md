# Mathematics

Status: COMPLETE

## SO.1 — strict spectral cap

For affine inverse squared radii, every inside label must satisfy `r_j^2<1`.
Self-checked dyadic lower radii for outside labels give
`rho_cap=min(rho_ceiling,min outside r_j^- - mu_rho)`. Positive safety makes
the outside inequality strict. With no outside label, the ceiling is used.

## SO.2 — amplitude and rank

AOPT maximizes total amplitude under this cap. Every point of its selected
amplitude subbox has radial envelope inside the cap, so every amplitude and knot
member has the frozen split. MKSPEC supplies the common exact projector, rank,
and conditioned resolvent.
