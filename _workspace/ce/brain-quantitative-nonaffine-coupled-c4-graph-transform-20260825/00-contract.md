# Research contract

Status: COMPLETE

PREDECESSOR: `_workspace/ce/brain-nonaffine-coupled-c3-extension-20260825`

## Objective

Close the global graph-dependent nonaffine coupled C4 graph transform with
base-map D4/D5 contributions, conditional C4,1 control, the exact five-layer
recurrence, affine-coupled and common-inverse-nonaffine reductions, and the
sharp equality obstruction.

## Frozen claims

Let `F_h=phi+f compose H_h`, `Y_h=B h+g compose H_h`, `L=(D F_h)^-1`,
and retain the predecessor tensors `T,N,M`. With `W=D4F_h`, `Z=D4Y_h`,

`O=Z-TW-4N[LU,.]-3N[LP,LP]-6M[LP,.,.]`

and `D4(T h)=O[L,L,L,L]`. The base-map D4 bound `U_phi` enters `W`; its
D5 modulus `V_phi` enters the point modulus and value-difference layer.

The generic graph-dependent route uses the modified tensor envelope. At the
exact structural boundary `L_fy=0`, the inverse is common to all graphs and a
direct inverse-chain route is mandatory to preserve the sharp coefficients.

## Claim ceiling

Conditional finite-dimensional theorem and exact apparatus only. No neural
field, empirical regularity, consciousness predicate, or dimension selection.

## Validation contract

Exact rational fixture, two independent reductions, the common-inverse
coefficient audit, D5 separation, strict equality counterexample, invalid
inputs, focused and adjacent regressions, dimensionless audit, compile, and
final research gate.
