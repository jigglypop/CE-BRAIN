# Mathematics

Status: COMPLETE

## AG.1 — automatic affine witnesses

Use the midpoint of the singular-sandwich inner and outer radii as a reference
circle. Complete Q factorization, unique primary partition, Bézout evaluation,
and exact complemented inversion construct P and R_Q. GADEF rechecks all
identities and the stronger whole-knot-box gates.

## AG.2 — interval rank

For GADEF bound `R_aff^+`, require `R_aff^+ epsilon^+<1`. Neumann inversion for
every `tE` preserves the resolvent and Riesz rank over the entire matrix ball,
affine contour family, and knot box.

## AG.3 — projector bound

Since `length/(2pi)<=S_L^+ C_spl^+`, resolvent identity gives
`||Delta P||<=S_L^+ C_spl^+ epsilon (R_aff^+)^2/(1-R_aff^+ epsilon)`.
