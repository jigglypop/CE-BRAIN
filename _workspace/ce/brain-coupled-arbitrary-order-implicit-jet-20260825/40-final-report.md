# Final report

Status: COMPLETE

The universal graph-dependent implicit-jet algebra is implemented and proved
for every explicitly supplied finite order.  The key correction is now fixed
in code and prose: the highest outer derivative belongs to the all-singleton
partition `(n,0,...,0)`, while graph-dependent inverse variation contributes
`n B_n alpha^(-(n+1)) delta F_1`.

This is a conditional apparatus, not yet the full coupled arbitrary-order
graph transform.  The next dependency is a map-specific raw-jet generator for
`F_h` and `Y_h`; local/matched collars and regularity growth remain later
layers.

Validation evidence is focused 19/19 and adjacent 129/129, including 50/50
dimensionless checks and both existing arbitrary-order branches.  Source
compile passed.  The run has exactly eight numbered records, no forbidden
markdown delimiter, and the final closure gate is `OK final`.
