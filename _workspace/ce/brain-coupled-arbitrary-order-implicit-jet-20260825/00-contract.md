# Research contract

Status: COMPLETE

PREDECESSOR: `_workspace/ce/brain-quantitative-nonaffine-coupled-c4-graph-transform-20260825`

## Objective

Isolate and prove the universal finite-order algebra that recovers the jets of
`T_h` from raw, graph-dependent jets in `Y_h=T_h compose F_h`, including the
complete two-graph coefficient recurrence and strict finite-order boundaries.

## Frozen claim

For each supplied finite order, the all-singleton Bell partition is the only
term containing the unknown highest outer jet.  A uniform conorm lower bound
therefore gives an exact recursive norm envelope.  Product telescoping plus
the n inverse slots gives the full upper-triangular difference row.

## Claim ceiling

Conditional algebraic implicit-jet solver only.  The supplied raw `F_h` and
`Y_h` jet envelopes still require a separate map-specific proof.  This run
does not claim a completed graph-dependent arbitrary-order graph transform,
local collars, C-infinity regularity, or a neural-data theorem.

## Validation contract

Exact C2--C4 Faà di Bruno identities, correct partition isolation, linear-base
reduction through C6, product/inverse-slot separation, strict-boundary failure,
synchronous iteration, dimension preservation, invalid-input rejection,
dimensionless audit, adjacent arbitrary-order regressions, compile, and final
closure gate.
