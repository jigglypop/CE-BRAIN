# Implementation

Status: COMPLETE

Runtime:

`reality_stone/python/reality_stone/clarus/verified_nonnormal_edge_disconnection_spectral_rank.py`

Focused test:

`tests/test_verified_nonnormal_edge_disconnection_spectral_rank.py`

The runtime uses exact `Fraction` matrix arithmetic, verifies the supplied inverse,
checks directed graph support, square-encloses Frobenius bounds without floating
point, enforces `rho<1` strictly, and exposes explicit false flags for defective
coverage and consciousness interpretation.
