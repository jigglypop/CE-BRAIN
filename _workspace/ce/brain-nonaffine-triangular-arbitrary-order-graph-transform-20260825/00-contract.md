# Research contract

Status: COMPLETE

PREDECESSOR: `_workspace/ce/brain-quantitative-nonaffine-c4-graph-transform-20260825`

## Objective

Extend the common-inverse nonaffine triangular graph transform to every
explicitly supplied finite order by an inverse Bell recurrence followed by a
second composition Bell recurrence, with exact C4 and affine reductions.

## Frozen claim

Forward base-map bounds D2phi..Dnphi recursively generate inverse bounds
I1..In from Dn(phi compose psi)=0. Raw graph-map derivative bounds and their
two-graph vectors are then transported through the common inverse by the full
set-partition formula. Every finite level requires class preservation and
strict q*mu^k<1.

## Claim ceiling

Conditional common-inverse nonaffine triangular finite-order theorem. No
graph-dependent inverse, local arbitrary-order collar, C-infinity, or neural
claim is included.

## Validation contract

Exact I2--I6, C4 predecessor identity, affine reduction at every level,
K(n+1) separation, synchronous iteration, input failures, dimensionless and
adjacent regressions, compile, and final gate.
