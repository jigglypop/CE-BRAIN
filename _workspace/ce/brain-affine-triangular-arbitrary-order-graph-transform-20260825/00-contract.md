# Research contract

Status: COMPLETE

PREDECESSOR: `_workspace/ce/brain-quantitative-triangular-c4-graph-transform-20260825`

## Objective

Replace copied C5+ candidate formulas by an exact finite-order
Faà di Bruno/Bell-polynomial hierarchy for affine triangular graph transforms,
including class radii, all recurrence coefficients, the K(n+1) modulus gate,
iteration, and a sharp equality counterexample.

## Frozen claim

For every supplied finite `n>=4`, exact normalized graph bounds D1..Dn and map
bounds K2..K(n+1), the integer-partition formula generates the Cn class and
the full `(n+1)`-layer recurrence. Every level must preserve its class and
satisfy strict `q*mu^k<1`. This is not a claim of C-infinity regularity.

## Claim ceiling

Conditional affine triangular finite-order theorem and exact apparatus. The
nonaffine, coupled, local arbitrary-order lift and all empirical neural claims
remain open.

## Validation contract

Exact C4 predecessor identity, partition/Bell counts, C5/C6 fixtures,
K(n+1) separation, synchronous iteration, equality witness, invalid inputs,
dimensionless and adjacent regressions, compile, and final gate.
