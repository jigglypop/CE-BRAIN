# Final report

Status: COMPLETE

The 4--6 statement is now a falsifiable held-out neural signal-rank protocol
rather than a preferred numerical assertion.  Multiple estimators, a complete
menu, paired state contrast, two-SE uncertainty, max-menu permutation, and
provenance are mandatory.

Only synthetic fixtures were run.  There is no current empirical 4--6 result,
and even a future signal-rank pass will not identify consciousness dimension.
The successor measurement-bridge audit additionally prevents a source-lock
string from setting an empirical flag without an authenticated observation
receipt.

Validation is focused 24/24 and adjacent 113/113, including 54/54
dimensionless checks.  Source compile passed.  The run has exactly eight
numbered records and the final closure gate is `OK final`.
