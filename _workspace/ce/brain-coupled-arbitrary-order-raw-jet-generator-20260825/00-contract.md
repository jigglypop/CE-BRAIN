# Research contract

Status: COMPLETE

PREDECESSOR: `_workspace/ce/brain-coupled-arbitrary-order-implicit-jet-20260825`

## Objective

Generate the raw finite-order jets and matched-preimage two-graph difference
rows for `F_h=f compose (Id,h)` and `Y_h=g compose (Id,h)` from normalized map
moduli, then connect them to the verified implicit-jet solver.

## Frozen claim

A preimage-value coupling bound, graph D1..Dn sizes plus the point modulus of
Dn, and map D1..Dn sizes/point moduli generate every raw Bell size and complete
D0..Dn difference row.  Substitution into the implicit recurrence gives a
conditional global coupled derivative-jet graph transform at every supplied
finite order.

## Claim ceiling

No C0/radius gate, local or matched boundary collar, optimized anisotropic C4
constant, C-infinity growth, neural map, empirical calibration, or selection
of dimension 4--6 is claimed.

## Validation contract

Exact raw C1--C4 Bell values, matched-preimage displacement rows, highest map
point-modulus separation, preimage-coupling routing, linear and curved C6
fixtures, strict boundary failure, dimension preservation, invalid-input
rejection, dimensionless and adjacent regressions, compile, and final gate.
