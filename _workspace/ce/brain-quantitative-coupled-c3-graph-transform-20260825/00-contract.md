# Research contract

Status: COMPLETE

Mode: light successor theorem

PREDECESSOR: `_workspace/ce/brain-quantitative-coupled-c2-graph-transform-20260825`

For the normalized affine coupled chart

$$
F_h(x)=Ax+a+f(x,h(x)),\qquad
Y_h(x)=Bh(x)+g(x,h(x)),
$$

derive and verify the narrow C3 successor without reusing the triangular C3
formula as if the base inverse were graph-independent. Promotion requires the
passing coupled C2 certificate, an invariant third-derivative class, a C3,1
class when $D_yf\ne0$, D4-level map moduli, and strict $Q/\alpha^3<1$.

`BIO_STARTING_MECHANISM`, `CE_DELTA`, `MEASUREMENT_MODEL`, and
`DATA_PROVENANCE` are unverified/not used. There is no data split or empirical
score. The residual rule is exact symbolic reduction plus rational tests. The
falsifier is $x'=x/2$, $y'=y/8$, $h_c=c|x|^3$. Matched controls are exact
zero-coupling reduction, optional C3,1 gate at $D_yf=0$, predecessor failures,
class equalities, scale covariance, and dimensions 1/4/5/6/100. The claim
ceiling is a conditional affine coupled C3 theorem/apparatus only.

