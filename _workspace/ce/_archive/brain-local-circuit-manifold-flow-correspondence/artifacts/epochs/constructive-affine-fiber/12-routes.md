# Constructive successor route audit

Status: COMPLETE

| Route | Status | Exact result or stop condition |
|---|---|---|
| A — affine contracting fiber | SELECTED / MATH CLOSED | C1--C6 construct $M$, attraction, $b$, sensitivity and a cost-conditioned metric under the frozen assumptions. |
| B — discrete-only skew product | VALID FALLBACK | If no base flow/logarithm is supplied, retain the exact invariant $M$ and $(M,\Phi|_M)$ but do not claim $b$. |
| C — mapping-torus suspension | VALID DIFFERENT-DIMENSION FALLBACK | An invertible discrete map may be suspended after adding a time dimension; this is not a vector field on the original $M$. |
| D — NHIM route | OPEN CONDITIONAL | Requires an explicit normal/tangent domination inequality in addition to fiber contraction. |
| E — nonsmooth forcing | STOP FOR $C^1$ | Bounded continuous $c$ gives a continuous graph; discontinuous $c$ does not justify a smooth manifold or $Dh$ formula. |

The selected route changes the mechanism, not a threshold or endpoint.  It
replaces an assumed invariant manifold with an explicit convergent graph series.
Its empirical falsifier is a source-locked circuit instance for which the frozen
block decomposition, contraction/bunching inequalities or held-out invariance
residual fails.

