# 구조 피벗 결과: r1-behavior-controlled-fiber

Status: COMPLETE

Outcome: EMPIRICAL STOP

## 판정

DANDI `001701@0.260120.0303`의 고정 자산에서 위치, 머리 방향, 100 ms 신경 발화량을 함께 사용했다. 후보는 행동 입력에 따라 섬유 선형부가 달라지는 모형이며, 단순 `base+input`, `full VAR+input`, `input-only`, 지속성, 1초 이동 행동 대조군과 비교했다.

후보 NMSE는 `0.9610331`이었다. 이는 `full VAR+input`의 `0.9607531`보다 `0.0291%` 나쁘고, `base+input`의 `0.9563235`보다 `0.4925%` 나쁘다. 후보 우위의 쌍체 블록 bootstrap 95% 구간은 각각 `[-0.2807, 0.2085]`, `[-2.6505, -0.1512]`로, 사전 고정한 양의 우위 조건을 통과하지 못했다.

평가 입력에서 계산한 섬유 연산자 최대 노름은 $q_{\max}=1.1061971>1$이다. 따라서 이 세션에서 행동으로 조절한 국소 섬유가 수축한다는 조건도 실패했다. 실제 행동은 1초 이동 행동보다 예측력이 있었지만, 이 사실은 제안한 섬유 구조의 우위나 수축성을 복원하지 않는다.

## 보존하는 좁은 결론

이 결과는 해당 세션과 동결한 모형류에서 R1을 중단시키는 유효한 음성 결과다. 신경 이력과 행동 입력에 시간 예측 정보가 있다는 관찰은 남지만, 제안한 행동-조절 섬유 구조는 더 단순한 대조 모형보다 낫지 않았다.

R1은 원래 전역 affine-fiber 결과를 본 뒤 제안한 outcome-informed 개발 경로이므로 독립 확인이 아니다. 내부 통과 조건이 실패했기 때문에 봉인한 DANDI `001695@0.260319.2023`은 열지 않았고 앞으로도 이 판정의 확인 자료로 사용하지 않는다.

## 감사와 재현

독립 상태 감사 판정은 `Gate: PASS — valid empirical STOP/FAIL, no apparatus defect found`다. 원 자산은 `12,967,760` bytes와 SHA-256 `5a2246041e421cd5b321adf9ccc40ba6f11379b40b08794c1b214590c50921f3`가 일치했으며, 계산 뒤 NWB는 삭제했다. 상세 수치는 `result.json`, 출처·삭제 영수증은 `source-receipt.json`, 실행 코드는 `analyze_behavior_pivot.py`에 있다.
