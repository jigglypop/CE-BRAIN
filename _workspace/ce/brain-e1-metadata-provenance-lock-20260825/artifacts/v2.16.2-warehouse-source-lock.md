# AllenSDK v2.16.2 warehouse source lock

Status: VERIFIED_SOURCE_ONLY

Access date: 2026-08-25

Pinned raw URL:

`https://raw.githubusercontent.com/AllenInstitute/AllenSDK/a9b5c685396126d9748f1ccecf7c00f440569f69/allensdk/brain_observatory/ecephys/ecephys_project_api/ecephys_project_warehouse_api.py`

Raw byte count: `13493`

Raw SHA-256: `c486110439af08e88ffc80a6abaa688dd7ae9768f77aef86d3ec0a3061186b0b`

The pinned class constructs its default `RmaEngine` with scheme `http` and
host `api.brain-map.org`. Its exact released method signature is:

```python
def get_sessions(
    self, session_ids=None, has_eye_tracking=None, stimulus_names=None
):
```

Its single `build_and_execute` query uses `criteria=model::EcephysSession`,
optional filters for the three arguments, and includes
`specimen(donor(age))` plus `well_known_files(well_known_file_type)`. The
method sets index `id`, derives age, sex, genotype and `has_nwb`, drops the
nested relationship objects, renames `stimulus_name` to `session_type`, and
returns the table. It contains no call to `get_units`, `get_channels`,
`get_probes`, `get_session_data`, `get_probe_lfp_data`, or a signal stream.

The included `well_known_files` relationship is file-presence metadata inside
the session RMA response. It is not an NWB download. A receipt may record that
the relationship was queried by the pinned method, but must discard download
links and must not make a separate WellKnownFile or stream request.

This artifact records source bytes and call semantics only. No Allen session
response, identifier list, file metadata response, NWB, unit, channel, probe,
spike, LFP, or scientific endpoint was fetched while creating it.
