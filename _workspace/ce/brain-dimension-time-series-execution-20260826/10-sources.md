# Sources and dependencies

Status: COMPLETE

No external dataset is accessed.  The executor consumes the existing complete-menu
dimension certificate and DSTAB gates.  Its finite mathematics uses orthogonal
projection reconstruction loss, a frozen linear rank penalty, and a conventional
cyclic moving-block resample schedule.  The run verifies supplied projector values
but not the upstream learning algorithm that produced them.
