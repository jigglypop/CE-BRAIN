# Alternative routes lane

Status: COMPLETE

## Route A: require exact matched boundaries for every dataset

Rejected as the only route.  Real measurements may support global or local
conditions without exact boundary anchoring.  The theorem target must be
frozen as GLOBAL, LOCAL, or MATCHED, with only its required schema enforced.

## Route B: treat hashes or provenance strings as evidence

Rejected.  Hashes establish byte identity and strings state intent; neither
authenticates a remote response or validates neural observations.

## Route C: reuse pointwise confidence intervals

Rejected.  The theorem consumes many constants across sessions and orders.
The complete family must be covered simultaneously and its exact multiplicity
must be declared.

## Selected route

Use canonical metadata identity, exact split matching, a frozen level-specific
parameter schema, one simultaneous envelope family, and fail-closed routing
through the existing certificates.  Join the rank apparatus only by explicit
held-out session IDs and permanently separate numerical equality from object
identity.
