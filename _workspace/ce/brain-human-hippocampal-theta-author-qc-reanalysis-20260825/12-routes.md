# BA-OBS-HPC2 alternative-route audit

Status: COMPLETE

`MODEL_SELECTION: NONE`. This light successor changes only the prespecified QC
transaction from the predecessor's baseline-first/50-percent aperture to the declared
author-order/`MIN20` aperture. It does not retune a threshold from the predecessor's
unreported failed count, change a cohort, change contacts, or use Nature aggregate output.
The completed hash-verified static receipt resolves the source-text prerequisite. It is
therefore a legitimate source-defined measurement successor, not an independent
confirmation and not a claim of exact MATLAB execution: p17/p19 uses the explicitly
frozen `(5,5,500)` measurement axiom after the archived invocation's defect was recorded.

| route | estimand and effective unit | remaining dof / target awareness | stop or falsifier |
|---|---|---|---|
| R1 author-order QC seal | fixed clinical/local P2P-of-mean late response; participant is unit (4 TS, 5 PB) | one locked late endpoint, two roles for reference (clinical primary; bipolar robustness); same public data has known published direction | static-receipt hash mismatch, fixture/source mismatch, or any reference/file below 20: no endpoint; clinical `D <= 0`: no support |
| R2 published model | trial P2P protocol-by-time mixed-model reproduction | trial clustering, random-effect and df grammar are different; outcome-aware reproduction target only | unavailable exact Satterthwaite engine: `PUBLISHED_MODEL_ENGINE_UNAVAILABLE`; cannot override R1 |
| R3 overlap-only paired sensitivity | p17/p19 within-person TS-PB change, effective n=2 | descriptive only; loses five unpaired participants | nonpositive value is a sensitivity flag; positive result cannot rescue R1 |
| R4 signed area/latency | signed mean-waveform functional | adds polarity/latency/window degrees of freedom and is target-aware after outcome access | prohibited; needs a distinct successor and new confirmation allocation |

R1 and R2 share trials, while R1 and R3 share p17/p19; none can be pooled as independent
evidence. The seven LOO values are influence diagnostics, not seven tests. They delete a
unique participant from every arm they occupy and recompute arm denominators. A sign flip
labels `PARTICIPANT_SENSITIVE`, never permits removing that participant.

The pre-specified comparison is author-order versus the predecessor's already terminal
baseline-first transaction. R0 (`HPC1_BASELINE_FIRST_50_PERCENT`) is a documented stop,
not a route to retry. R2 (`COUNT_TUNED_THRESHOLD`) is prohibited: no post-stop clean-count
observation may select a new artifact threshold. R3 (`SOURCE_DATA_TARGET_MATCH`) is also
prohibited: the Nature aggregate cannot calibrate raw reading or QC.

No alternate route supplies anatomical connectivity, a healthy-population estimate,
memory behavior, a metric, consciousness, or AGI. No condition-label permutation is
valid because the participant protocol assignment is not treated as exchangeable.
