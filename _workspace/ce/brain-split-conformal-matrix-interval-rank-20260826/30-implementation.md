# Implementation

Status: COMPLETE

Implemented:

- `verified_split_conformal_matrix_uncertainty_coverage.py`;
- `verified_conformal_interval_characteristic_bridge.py`.

Receipts expose calibration/held-out scores, exact order index and coverage lower,
component radii, exact binomial tail, full component schema, reshaped matrix box,
IVSPEC receipt/rank, and nonpromotion flags.
