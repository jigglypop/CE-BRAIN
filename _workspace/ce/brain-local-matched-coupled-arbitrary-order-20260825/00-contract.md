# Research contract

Status: COMPLETE

PREDECESSOR: `_workspace/ce/brain-coupled-arbitrary-order-raw-jet-generator-20260825`

## Objective

Lift the global conditional coupled finite-order derivative hierarchy to a
backward-covered local chart and then to an exact matched input/output domain,
with order-uniform collar and boundary semantics.

## Frozen claim

If the same normalized map/graph moduli cover the open input collar through
the required Dn point-modulus order, core and collar inverse coverage compose
with the global derivative certificate.  Exact inverse/forward contacts and
anchored graph/fiber boundaries then give a matched-domain wrapper.

## Claim ceiling

The collar-modulus coverage is a declared mathematical hypothesis, not an
empirical verification.  Exact contacts are not robust interior margins.  No
C-infinity growth, neural chart, biological boundary, or dimension selection
is claimed.

## Validation contract

Core/collar separation, one-extra-order coverage, C2/C4/C6 genericity,
independent named failures, equality semantics, exact matched contacts,
boundary anchoring, scale covariance, synchronous iteration, dimension
preservation, invalid inputs, dimensionless and adjacent regressions, compile,
and final gate.
