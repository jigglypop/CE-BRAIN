# Mathematics

Status: COMPLETE

## DO.1 — algebraic radial cap

Zero-amplitude AIGADEF discovers exact P and R_Q. With exterior inverse norm
`b^+`, affine upper `S_L^+`, and safety `mu_out`, set
`rho_cap=min(rho_ceiling,(1-mu_out)/(S_L^+ b^+))`. This guarantees final GADEF
exterior reciprocal gap at least `mu_out` throughout the selected amplitude box.

## DO.2 — exact amplitude product box

AOPT gives the exact total-amplitude optimum and whole selected subbox. The
inside unit-core gate is unchanged by nonnegative amplitudes; the algebraic cap
controls the outside block. Therefore all amplitude and knot members share the
automatic defective projector, rank, and algebraic resolvent.

## DO.3 — matrix product family

If the final algebraic resolvent bound times normalized matrix uncertainty is
strictly below one, Neumann homotopy preserves rank for the entire third product
factor and the usual length/two-resolvent formula bounds projector perturbation.
