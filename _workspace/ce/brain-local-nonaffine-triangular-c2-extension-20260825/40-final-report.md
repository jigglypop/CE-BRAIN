# Local graph-independent nonaffine triangular C2 graph transform

Status: COMPLETE

Date: 2026-08-25

## Abstract

The global nonaffine triangular C2 theorem is localized without silently
assuming that every forward base point stays inside the chart. A certified
inverse-domain inclusion makes the graph transform a self-map over local
convex domains. Its fixed graph is C2 with backward-covered overflow
invariance. Contracting and expanding base controls respectively show why
coverage is necessary and why it does not imply full forward retention. This
is a conditional mathematical result, not a neural or consciousness result.

## 1. Local domain gate

For $U_t=\overline B(c_t,r_t)$ and $\psi_t=\phi_t^{-1}$, require

$$
\psi_t(U_{t+1})\subseteq U_t.
$$

The exact scalar certificate is

$$
r_{\rm pre}
=\sup_{x'\in U_{t+1}}\|\psi_t(x')-c_t\|
\le r_t,
\qquad
m_{\rm cov}=r_t-r_{\rm pre}\ge0.
$$

This makes every evaluation $h_t(\psi_t(x'))$ legal. Equality passes but is
non-robust.

## 2. Local C2 theorem

Assume the maps and graphs extend C2 to open neighborhoods, the base domains
are convex, and all predecessor bounds hold uniformly there. If inverse
coverage, the nonaffine C1 gate, Hessian-class invariance, and strict
$q\mu^2<1$ pass, then the unique local fixed graph is C2. The class bound and
recurrence are exactly the global predecessor formulas because their proof is
pointwise after the common preimage is known to lie in the domain.

The invariant meaning is restricted:

$$
F_t(\operatorname{graph}h_t)
\cap(U_{t+1}\times Y)
=\operatorname{graph}h_{t+1}.
$$

It does not assert $\phi_t(U_t)\subseteq U_{t+1}$.

## 3. Exact curved fixture and falsifiers

For $\phi_{\lambda,a}(x)=\lambda x+a\sin x$ with $\lambda>a\ge0$,

$$
\mu=(\lambda-a)^{-1},
\qquad
\nu=a(\lambda-a)^{-3},
\qquad
r_{\rm pre}\le\mu r_t.
$$

At $(\lambda,a)=(2,1/4)$ and $r_t=1$, the strict certificate has

$$
m_{\rm cov}=\frac37,
\quad
\Lambda_{2,\mathrm{out}}=\frac{94}{343},
\quad
\beta_2=\frac8{49},
\quad
c_{21}=\frac{36}{343},
\quad
c_{20}=\frac{37}{343}.
$$

On $[-1,1]$, $\phi(x)=x/2$ fails because $x'=3/4$ requires the undefined
local value $h(3/2)$. Conversely $\phi(x)=2x$ passes inverse coverage but sends
$x=3/4$ outside the output domain. These controls fix both sides of the claim
boundary.

## 4. Evidence ceiling and reproducibility

This closes the backward-covered/overflow local graph-independent nonaffine
triangular C2 route. Exact matched-domain full forward retention, nonaffine
coupled C2, C3+, an observed neural chart, consciousness, and 4--6 dimension
selection remain open. Focused, adjacent, and integration counts are recorded
in `31-validation.md`; no external empirical source was used.

## References

The formal dependency is the final-gated global graph-independent nonaffine
triangular C2 repository run. No literature or empirical data is introduced.

