# Formal audit

Status: COMPLETE

Gate: PASS

| Claim | Status | Audit result |
|---|---|---|
| Local inverse-domain coverage | CONDITIONAL THEOREM | The supplied inverse-image radius bound is sufficient; equality is non-robust. |
| Overflow local C2 graph | CONDITIONAL THEOREM | The predecessor proof localizes because all evaluations and segments remain in the declared convex domains. |
| Full forward retention | REJECTED PARENT CLAIM | The expanding-base counterexample proves inverse coverage is insufficient. |
| Coverage omission | COMPLETE COUNTEREXAMPLE | The contracting-base example evaluates the graph outside its domain. |
| Expanding sine witness | DERIVED | Exact $\mu,\nu,r_{\rm pre}$ and strict rational fixture are proved. |
| Local certificate apparatus | APPROVED FOR IMPLEMENTATION | Must preserve predecessor failures and report overflow invariance explicitly. |
| Coupled/C3+/brain promotion | INCOMPLETE | Required inverse comparisons, higher derivatives, and empirical premises are absent. |

The radius and inverse-image radius have base-coordinate units before division
by the same positive reference scale. Every recurrence coefficient is
dimensionless only in the normalized chart. The implementation cannot verify
that a supplied map actually has the declared bounds; it only checks the exact
conditional certificate. It must never label inverse coverage as full forward
domain invariance.

