# Routes

Status: COMPLETE

root fingerprint -> root-signed coverage-key anchor -> native/converter/output digest
message -> coverage-key signature -> SCIDISC conditional rank receipt.

Remaining: independently publish the root fingerprint, attach a hardware/TEE execution
attestation, and run the chain on a replicated human intervention.
