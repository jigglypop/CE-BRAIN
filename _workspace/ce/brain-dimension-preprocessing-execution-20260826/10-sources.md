# Sources and dependencies

Status: COMPLETE

No external source is accessed.  The run uses elementary exact centering and
coordinate normalization, then consumes DPCA, DEXEC, and DSTAB.  Reference scales
are frozen inputs rather than estimates from held-out data.  Hash strings identify
caller payloads but do not authenticate source bytes.
