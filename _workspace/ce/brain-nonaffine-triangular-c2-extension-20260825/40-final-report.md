# Quantitative nonaffine triangular-base C2 graph transform

Status: COMPLETE

Date: 2026-08-25

## Abstract

The global nonaffine C1 theorem and affine triangular C2 theorem are combined
without dropping the curvature of the inverse base map. The mandatory added
term is $DS_hD^2\psi_t$. Exact inverse derivative and Hessian bounds close an
invariant C2 graph class and a three-level contraction recurrence. A
sine-perturbed global diffeomorphism provides a concrete exact witness. This
is a conditional mathematical theorem, not a neural or consciousness result.

## 1. The nonaffine term

For $\mathcal Th=S_h\circ\psi_t$,

$$
D^2(\mathcal Th)=D^2S_h[D\psi_t,D\psi_t]+DS_hD^2\psi_t.
$$

With $\|D\psi_t\|\le\mu$, $\|D^2\psi_t\|\le\nu$,
$q=b+L_y$, and $s=q\kappa+L_x$, the invariant-class bound is

$$
\Lambda_{2,\mathrm{out}}^{\rm na}
=\mu^2\{q\Lambda_2+K_2(1+\kappa)^2\}+s\nu.
$$

## 2. Conditional theorem

If the global nonaffine C1 certificate passes, the displayed output bound is
at most $\Lambda_2$, $K_3$ is finite, and $q\mu^2<1$, then the unique invariant
graph is C2. For two iterates,

$$
e_{n+1}\le q\mu^2 e_n
+\{2\mu^2K_2(1+\kappa)+q\nu\}d_n
+\left[\mu^2\{K_2\Lambda_2+K_3(1+\kappa)^2\}
+\nu(H_y\kappa+H_x)\right]\delta_n.
$$

The inverse map is common to both graphs, so no two-graph modulus of
$D^2\psi_t$ is needed. This is precisely why the result does not yet cover a
coupled nonaffine base.

## 3. Exact curved witness

For $\phi_a(x)=x+a\sin x$, exact $0\le a<1$ gives

$$
\mu_a=(1-a)^{-1},\qquad \nu_a=a(1-a)^{-3}.
$$

At $a=1/4$, the strict fixture has

$$
\Lambda_{2,\mathrm{out}}^{\rm na}=214/27<8,
\quad\beta_2=8/9,
\quad c_{21}^{\rm na}=20/27,
\quad c_{20}^{\rm na}=38/27.
$$

At $a=0$ the apparatus reduces exactly to affine triangular C2. At
$q\mu^2=1$, the existing $h_c(x)=cx|x|$ counterexample prevents a C2 claim.

## 4. Evidence ceiling

This closes global graph-independent nonaffine triangular C2 in normalized
coordinates. It does not close nonaffine coupled/local C2, C3+, a concrete
neural field, biological measurement, consciousness, or selection of 4--6
dimensions. The code preserves any supplied positive dimension, including
$1,4,5,6,100$; that is compatibility, not evidence for one of them.

## References

Formal dependencies are the final-gated global nonaffine triangular C1 and
affine triangular C2 repository runs. No external empirical source is used.

