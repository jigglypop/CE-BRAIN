# Mathematics

Status: COMPLETE

## DDISC.1 — algebraic split

Require `P^2=P`, `[P,U0]=0`, `Q=I-P`, `B=QBQ`, and
`B(U0-cI)Q=(U0-cI)B=Q`. This admits defective invariant blocks.

## DDISC.2 — base resolvent

If `a^+>=||(U0-cI)P||`, `a^+<r`, `b^+>=||B||`, and `rb^+<1`, then

`R_alg^+=||P||^+/(r-a^+) + b^+/(1-rb^+)`

bounds the resolvent on the circle. The two terms are the inside and exterior
geometric-series bounds.

## DDISC.3 — directed-edge homotopy

For normalized `epsilon^+>=kmax||E||`, `R_alg^+ epsilon^+<1` preserves the
fixed-circle Riesz rank and gives

`||P1-P0|| <= r (R_alg^+)^2 epsilon^+/(1-R_alg^+epsilon^+)`.

The rank-six Jordan fixture has inside gap 1, exterior reciprocal gap 1/2,
`R_alg^+=3/2`, `epsilon^+=1/100`, `rho=3/200`, and movement bound `9/197`.

## DDISC.4 — boundaries

Equality `rho=1`, invalid projectors/inverses, support mismatch, and underreported
coupling fail closed. Gate failure is not evidence of crossing.
